<nav class="navbar has-shadow">
    <div class="container">
        <div class="navbar-start">
            <a href="{{ path('homepage') }}"
               class="navbar-item is-tab {{ 'homepage' | active('is-active') }}">
                MyKeep
            </a>
        </div>
        <div class="navbar-center">
            <span class="navbar-item">
                {% if app.request.attributes.get('_route') == 'homepage' %}
                    <a href="#test-popup" class="button is-primary open-popup-link">
                        <span class="icon">
                            <i class="fa fa-plus"></i>
                        </span>
                        <span>{{ 'note.add_new' | trans }}</span>
                    </a>
                {% endif %}
            </span>
        </div>
        <div class="navbar-end">
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <a href="{{ path('note_trash') }}"
                   class="navbar-item is-tab {{ 'note_trash' | active('is-active') }}">
                    {{ 'trash' | trans }}
                </a>
                <a class="navbar-item is-tab">
                    {{ app.user.username }}
                </a>
                <a href="{{ path('fos_user_security_logout') }}"
                   class="navbar-item is-tab">
                    {{ 'logout' | trans }}
                </a>
            {% else %}
                <a href="{{ path('fos_user_registration_register') }}"
                   class="navbar-item is-tab {{ 'fos_user_registration_register' | active('is-active') }}">
                    {{ 'registration' | trans }}
                </a>
                <a href="{{ path('fos_user_security_login') }}"
                   class="navbar-item is-tab {{ 'fos_user_security_login' | active('is-active') }}">
                    {{ 'login' | trans }}
                </a>
            {% endif %}
        </div>
    </div>
</nav>